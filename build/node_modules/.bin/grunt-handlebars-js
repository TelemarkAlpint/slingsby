#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-handlebars-js/bin/grunt-handlebars-js" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-handlebars-js/bin/grunt-handlebars-js" "$@"
  ret=$?
fi
exit $ret
