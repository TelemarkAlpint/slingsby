#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-templater/bin/grunt-templater" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-templater/bin/grunt-templater" "$@"
  ret=$?
fi
exit $ret
